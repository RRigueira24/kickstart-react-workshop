import styled from "@emotion/styled";

const CharacterListWrapper = styled.div`
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  max-width: 1280px;
  margin: 0 auto;
`;

export { CharacterListWrapper };
